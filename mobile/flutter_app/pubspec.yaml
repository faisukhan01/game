name: voidstrike
description: "VOIDSTRIKE — Arena Protocol. Cross-platform twin-stick arena shooter (Flutter + Flame)."
publish_to: "none"
version: 1.1.0+2

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.18.0
  web_socket_channel: ^3.0.0
  flutter_riverpod: ^2.5.1
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
